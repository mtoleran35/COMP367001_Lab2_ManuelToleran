pipeline {
    agent any
	
	//Stages
    stages {
        // Checkout Stage
        stage('Checkout') {
            steps {
				checkout scm 
            }
        }

        // Build Stage
        stage('Build') {
            steps {
				script {
                    sh 'mvn -f ManuelToleran_COMP367001_Lab2_Q2/pom.xml install'
                }
                echo 'Building application...'
            }
        }

    }
}